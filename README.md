# rznish — photo gallery

## Run it

```bash
npm install
cp .env.example .env.local   # both values are optional
npm run dev                  # http://localhost:3000
```

## Adding photos

`src/data/gallery.ts`. `albumSlug`

