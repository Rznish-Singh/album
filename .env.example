# Public base URL, used for OG tags and share links.
NEXT_PUBLIC_SITE_URL=https://gallery.rznish.dev

# Map tiles. Leave empty to use the keyless OpenStreetMap raster fallback.
# Set a MapTiler key to get the dark vector basemap instead.
NEXT_PUBLIC_MAPTILER_KEY=
